# 설정 가이드

전부 무료입니다. 신용카드는 어디에도 등록하지 않습니다.
**PC와 휴대폰이 모두 꺼져 있어도 매일 오전 7시에 카톡이 옵니다.** GitHub 서버가 대신 실행하기 때문입니다.

총 소요 시간 약 25분. 순서대로 따라 하시면 됩니다.

---

## 준비물 요약

| 단계 | 무엇을 | 얼마나 |
|---|---|---|
| 1 | GitHub 계정 만들고 저장소에 파일 올리기 | 8분 |
| 2 | Gemini API 키 발급 | 3분 |
| 3 | 카카오 개발자 앱 등록 + 토큰 발급 | 10분 |
| 4 | GitHub Secrets 등록 | 3분 |
| 5 | GitHub Pages 켜고 주소 넣기 | 3분 |
| 6 | 테스트 실행 | 2분 |

---

## 1단계 — GitHub 저장소 만들기

1. https://github.com 접속 → **Sign up** → 이메일·비밀번호로 가입 (무료)
2. 로그인 후 오른쪽 위 **+** → **New repository**
3. 아래처럼 입력합니다.
   - **Repository name**: `etf-morning-brief`
   - **Private** 선택 ← ⚠️ 반드시 Private으로 하세요. 토큰이 오갑니다.
   - **Add a README file** 체크 해제
   - **Create repository** 클릭
4. 다음 화면에서 **uploading an existing file** 링크 클릭
5. 받으신 `etf-morning-brief.zip`의 **압축을 풀고**, 폴더 안의 내용물을 통째로 드래그해서 올립니다.
   - ⚠️ zip 파일 자체가 아니라 **압축을 푼 안의 파일들**을 올려야 합니다.
   - ⚠️ `.github` 폴더가 빠지면 자동 실행이 안 됩니다. 숨김 폴더라 안 보이면
     Windows는 탐색기 **보기 → 숨긴 항목** 체크, Mac은 `Cmd + Shift + .` 를 누르세요.
6. 아래 **Commit changes** 클릭

> **Private 저장소에서도 GitHub Actions는 매달 2,000분까지 무료입니다.**
> 이 브리핑은 하루 2~3분이라 월 90분 정도 씁니다. 무료 한도 안에서 충분합니다.

---

## 2단계 — Gemini API 키 발급 (무료)

브리핑의 요약과 '해석' 부분을 만드는 AI입니다.

1. https://aistudio.google.com/apikey 접속
2. 구글 계정으로 로그인
3. **Create API key** 클릭 → **Create API key in new project**
4. 나온 키를 복사해 메모장에 붙여둡니다. (`AIza...`로 시작)

> **비용**: 무료 티어는 하루 1,500회 요청까지 무료이고 만료가 없습니다.
> 이 브리핑은 하루 1~2회만 쓰므로 요금이 청구될 일이 없습니다.
> 단, 무료 티어는 입력 내용이 구글의 모델 개선에 사용될 수 있습니다.
> 우리가 넣는 건 공개된 뉴스와 시세뿐입니다.

---

## 3단계 — 카카오 토큰 발급

가장 까다로운 단계입니다. 천천히 따라 하세요.

### 3-1. 카카오 개발자 앱 만들기

1. https://developers.kakao.com 접속 → 카카오 계정으로 로그인
2. 상단 **내 애플리케이션** → **애플리케이션 추가하기**
3. 입력:
   - 앱 이름: `ETF 아침 브리핑`
   - 사업자명: 본인 이름 또는 채널명
   - 카테고리: 아무거나
4. **저장** 클릭

### 3-2. REST API 키 복사

1. 만든 앱 클릭 → 왼쪽 메뉴 **앱 키**
2. **REST API 키** 를 복사해 메모장에 붙여둡니다.

### 3-3. 카카오 로그인 켜기

1. 왼쪽 메뉴 **카카오 로그인**
2. **활성화 설정**을 **ON**
3. 같은 화면 아래 **Redirect URI 등록** 클릭 → `https://localhost:3000` 입력 → 저장
   - 실제로 접속되는 주소가 아니어도 됩니다. 형식만 맞으면 됩니다.

### 3-4. 메시지 전송 권한 켜기

1. 왼쪽 메뉴 **카카오 로그인 > 동의항목**
2. 목록에서 **카카오톡 메시지 전송** (`talk_message`) 을 찾습니다.
3. 오른쪽 **설정** → **이용 중 동의** 선택 → 저장

### 3-5. 인증 코드 받기

아래 주소의 `여기에_REST_API_키` 부분을 3-2에서 복사한 키로 바꾼 뒤,
브라우저 주소창에 붙여넣고 접속합니다.

```
https://kauth.kakao.com/oauth/authorize?client_id=여기에_REST_API_키&redirect_uri=https://localhost:3000&response_type=code&scope=talk_message
```

1. 카카오 동의 화면이 뜨면 **동의하고 계속하기**
2. 그러면 **"사이트에 연결할 수 없음"** 같은 오류 페이지로 넘어갑니다. **정상입니다.**
3. 이때 **주소창**을 보세요. 이렇게 생겼습니다.

```
https://localhost:3000/?code=AbCdEf1234567890...
```

4. `code=` **뒤의 값만** 복사합니다. (`AbCdEf1234...` 부분)
   - 이 코드는 **10분 안에, 딱 한 번만** 쓸 수 있습니다. 바로 다음 단계로 가세요.

### 3-6. 리프레시 토큰 받기

먼저 4단계로 가서 `KAKAO_REST_API_KEY` **하나만** 등록하고 오세요. 그다음:

1. GitHub 저장소 → 상단 **Actions** 탭
2. 왼쪽 목록에서 **🔑 카카오 토큰 발급 (최초 1회)** 클릭
3. 오른쪽 **Run workflow** 버튼 클릭
4. `code` 칸에 3-5에서 복사한 값을 붙여넣기
5. **Run workflow** 클릭 → 1분쯤 기다린 뒤 실행 기록 클릭 → 로그 펼치기
6. 로그에 이렇게 나옵니다.

```
==================================================================
  성공했습니다. 아래 값을 통째로 복사하세요.
==================================================================

여기에_긴_토큰_값이_나옵니다

==================================================================
```

7. 이 값을 복사해서 4단계의 `KAKAO_REFRESH_TOKEN` 으로 등록합니다.
8. 등록이 끝나면 이 실행 기록은 삭제하세요. (실행 기록 오른쪽 위 ⋯ → Delete workflow run)

> 실패했다면 로그에 원인이 적혀 있습니다. 가장 흔한 건 **code를 이미 한 번 썼거나 10분이 지난 경우**입니다.
> 3-5부터 다시 하면 됩니다.

---

## 4단계 — GitHub Secrets 등록

저장소 → **Settings** → 왼쪽 **Secrets and variables** → **Actions** → **New repository secret**

아래를 하나씩 등록합니다.

| Name (정확히 이대로) | Secret (값) | 필수 |
|---|---|---|
| `KAKAO_REST_API_KEY` | 3-2에서 복사한 REST API 키 | ✅ |
| `KAKAO_REFRESH_TOKEN` | 3-6에서 받은 토큰 | ✅ |
| `GEMINI_API_KEY` | 2단계에서 받은 키 | ✅ |
| `YOUTUBE_API_KEY` | 5단계 참고 (없으면 유튜브 섹션만 생략됨) | ⬜ |
| `GH_PAT` | 아래 설명 참고 | ⬜ 권장 |
| `KAKAO_CLIENT_SECRET` | 카카오 앱에서 client_secret을 켠 경우만 | ⬜ |

### GH_PAT 는 왜 권장인가

카카오 리프레시 토큰은 유효기간이 약 60일입니다. 시스템이 매일 토큰을 갱신하는데,
만료가 가까워지면 카카오가 **새 토큰**을 내줍니다. `GH_PAT`가 있으면 이 새 토큰을
Secret에 자동으로 덮어씁니다. 없으면 두 달에 한 번 3단계를 다시 해야 합니다.

만드는 법:

1. https://github.com/settings/personal-access-tokens/new
2. **Token name**: `brief-secret-updater`
3. **Expiration**: `No expiration` 또는 1년
4. **Repository access**: **Only select repositories** → `etf-morning-brief` 선택
5. **Permissions** → **Repository permissions** → **Secrets** 을 찾아 **Read and write** 로 설정
6. **Generate token** → 나온 값을 `GH_PAT` Secret 으로 등록

---

## 5단계 — GitHub Pages 켜기 (전문 보기 링크용)

카톡 메시지에 붙는 "전문 보기" 링크가 열리는 웹페이지입니다.

1. 저장소 → **Settings** → 왼쪽 **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `main` / 폴더는 **`/docs`** 선택 → **Save**
4. 1~2분 뒤 화면 위에 주소가 나옵니다. 이런 형태입니다.
   ```
   https://내아이디.github.io/etf-morning-brief
   ```
5. 저장소 첫 화면으로 돌아가 **`config.yaml`** 파일 클릭 → 연필 아이콘(Edit)
6. 맨 위쪽 `사이트_주소:` 줄을 찾아 방금 주소를 넣습니다.
   ```yaml
   사이트_주소: "https://내아이디.github.io/etf-morning-brief"
   ```
7. **Commit changes**

> Private 저장소에서 GitHub Pages를 쓰려면 Pro 요금제가 필요합니다.
> 무료로 쓰시려면 저장소를 Public으로 바꾸되, **Secrets는 Public에서도 공개되지 않으니 안전합니다.**
> 다만 브리핑 내용이 인터넷에 공개되는 게 신경 쓰이면, 링크 없이 카톡 2건만 받는 방식으로
> `config.yaml`의 `링크_붙이기: false` 로 두셔도 됩니다.

### YouTube API 키 (선택)

유튜브 트렌드 섹션을 쓰려면 필요합니다. 없으면 그 섹션만 조용히 빠집니다.

1. https://console.cloud.google.com/apis/credentials 접속
2. 프로젝트 만들기 → **사용자 인증 정보 만들기** → **API 키**
3. 왼쪽 **라이브러리** → `YouTube Data API v3` 검색 → **사용 설정**
4. 만든 키를 `YOUTUBE_API_KEY` Secret 으로 등록

---

## 6단계 — 테스트 실행

1. 저장소 → **Actions** 탭
2. 왼쪽에서 **아침 브리핑 (매일 07:00 KST)** 클릭
3. **Run workflow** → `카톡 발송 없이 HTML만 생성` 을 **체크** → **Run workflow**
4. 3~5분 기다립니다. 초록 체크가 뜨면 성공입니다.
5. 5단계에서 만든 주소로 접속해 브리핑이 제대로 나오는지 확인합니다.
6. 잘 나오면 **체크를 풀고 다시 실행**해서 카톡이 오는지 확인합니다.

빨간 X가 뜨면 실행 기록을 클릭해 로그를 보세요. 어느 단계에서 멈췄는지 한국어로 적혀 있습니다.

---

## 이후 운영

자동 발송 일정은 세 가지입니다.

| 언제 | 무엇 | 카톡 |
|---|---|---|
| 매일 오전 7시 | 아침 브리핑 | 2건 |
| 목요일 오전 11시 | ETF 처방전 전달문 (뉴스 6선 · 전문가 발언 · 출연자 추천) | 1건 |
| 토요일 오전 7시 | 주간 브리핑 | 2건 |

지난 자료는 전부 사이트에 쌓입니다. 주소 뒤에 아무것도 안 붙이면 목록이 나옵니다.

**목요일 전달문 사용법** — 오전 11시에 카톡 알림이 오면 링크를 열고,
`📋 복사용 텍스트 열기`를 눌러 전체 선택 후 복사해서 실장님께 카톡으로 붙여넣으시면 됩니다.

### 내용을 바꾸고 싶을 때

`config.yaml` 파일만 고치면 됩니다. 코드는 건드릴 필요 없습니다.

| 바꾸고 싶은 것 | 고칠 곳 |
|---|---|
| 종목이 너무 많이/적게 나온다 | `종목_편성기준 > 등락률_임계치` (숫자를 키우면 덜 나옴) |
| ETF 레이더 항목 수 | `ETF_레이더 > 최대_항목수` |
| 추적할 유튜브 채널 | `유튜브 > 채널` 목록에 추가/삭제 |
| 카톡을 1건만 받고 싶다 | `카카오 > 발송건수: 1` |
| 아침 브리핑 시각 | `.github/workflows/daily.yml` 의 `cron: "0 22 * * *"` <br>(UTC 기준. 한국시간 -9시간. 예: 오전 6시 원하면 `0 21 * * *`) |
| 목요일 전달문 시각 | `.github/workflows/thursday.yml` 의 `cron: "0 1 * * 4"` <br>(오전 10시 = 01:00 UTC. 오후 2시로 옮기려면 `0 5 * * 4`) |
| 전달문 뉴스 수집 범위 | `config.yaml` 의 `목요일_전달문 > 최근_뉴스_시간` (기본 96시간) |
| 전달문 뉴스 건수 | `config.yaml` 의 `목요일_전달문 > 뉴스_선정개수` |
| 뉴스 소스 추가 | `뉴스` 섹션에 RSS 주소 추가 |

### 문제가 생기면

- 카톡이 안 온다 → Actions 탭에서 최근 실행 로그 확인
- "카카오 토큰 갱신 실패" → 3-5부터 다시 (10분이면 끝납니다)
- 브리핑 내용이 이상하다 → `debug/` 폴더에 그날 수집한 원본 데이터와 AI 응답이 저장됩니다
